var rest = require('restler');
var sys = require('util');
//console.log(rest.get(String(1)).on('complete', function(data) {
 // sys.puts(data[0].message); // auto convert to object
//}));
console.log(rest.get(String(1)));
